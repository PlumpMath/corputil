language: python
python:
    - "3.4"
    - "3.5"
before_install:
    - pip install pytest pytest-cov
install:
    - python setup.py install
after_install:
    - python -m nltk.downloader punkt
script:
    - py.test